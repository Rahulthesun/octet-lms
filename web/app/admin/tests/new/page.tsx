'use client'

import ScheduleTestForm from '@/components/admin/tests/ScheduleTestForm'

// Full-page "Schedule New Test" (opens inside the admin layout, with the sidebar).
export default function NewTestPage() {
  return <ScheduleTestForm />
}
